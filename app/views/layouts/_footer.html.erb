<div id="footer-menu" class="btn-group btn-group-justified">
  <%= link_to "Home", "#", class: "btn btn-primary" %>
  <a href="#" class="btn btn-primary">👥Every</a>

  <% if logged_in? %>
    <%= link_to "👤You", current_user, class: "btn btn-primary" %>

    <div class="btn-group dropup">
      <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        ⚙️Account<span class="caret"></span>
      </button>
      <div class="dropdown-menu dropdown-menu-right" role="menu">
        <%= link_to "Users", users_path, class: "btn btn-info" %>
        <%= link_to "Settings", edit_user_path(current_user), class: "btn btn-info" %>
        <%= link_to "Log out", session_path(current_user), method: :delete, class: "btn btn-info" %>
      </div>
    </div>
    
  <% else %>
    <%= link_to "👤You", new_session_path, class: "btn btn-primary" %>
    <div class="btn-group dropup">
    <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      ⚙️Account<span class="caret"></span>
    </button>
    <div class="dropdown-menu dropdown-menu-right" role="menu">
      <%= link_to "Log in", new_session_path, class: "btn btn-primary" %>
    </div>
  </div>

  <% end %>
</div>